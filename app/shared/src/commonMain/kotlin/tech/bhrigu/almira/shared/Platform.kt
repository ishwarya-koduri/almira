package tech.bhrigu.almira.shared

/**
 * The first thing every KMP project needs: one declaration, two answers.
 *
 * It earns its place beyond saying hello — the platform name goes on screen in
 * the skeleton, so running the app is itself the proof that the shared module
 * is being served by the right actual implementation on each target.
 */
expect fun platformName(): String
